import { BasicButton } from "../../../base";
import styled from "@emotion/styled";

/** Rounded Styling applied to BasicButton Component, via EmotionJS */
export const RoundStyling = styled(BasicButton)`
  border-radius: 16px;
`;
